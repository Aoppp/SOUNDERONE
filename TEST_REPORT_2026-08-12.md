# SOUNDERONE Agent 自动化测试报告

## 1. 结论

**测试结论：未通过，不建议以当前版本进入抖音灰度。**

- 全量自动化共收集 **115** 项：**114 通过、1 失败**，通过率 **99.13%**。
- 新增的业务验收集共 **52** 项：**51 通过、1 失败**，通过率 **98.08%**。
- 唯一失败为 `FAQ-009`：`AM质地为什么这么稀`。知识库存在正确 FAQ 且检索排名第一，但请求在进入 RAG 前被范围路由拦截。
- 主动转人工、强负面情绪、高危规则、数值准确性、主要推荐/对比/搭配、上下文、鉴权、幂等和脱敏检查均通过。
- FAQ 本地直答 P95 为 **22.8ms**，低于 `TEST_CASES.md` 规定的 1 秒；真实 DeepSeek 3 次采样 P50/P95 为 **2263.4/3167.1ms**。
- `TEST_CASES.md` 中共有 161 个业务用例编号。本轮新增验收测试可直接追溯到 54 个编号，其余用例由既有单元/集成测试部分覆盖，但尚未逐项建立 ID 映射；100 会话并发、同会话 20 轮、真实抖音回调和至少 50 条真实客服问题人工复核未执行。

自动化命令返回非零退出码，因此即使总体通过率超过 95%，仍不满足“自动化测试全部通过”的发布条件。

## 2. 测试基线

| 项目 | 内容 |
|---|---|
| 执行日期 | 2026-08-12 |
| Git 基线 | `773c7b2` |
| 运行环境 | macOS 26.5.1，Python 3.11.9 |
| Agent 运行时 | LangGraph |
| LLM | DeepSeek / `deepseek-v4-flash` |
| Embedding | Hash embedding |
| 检索 | Qdrant Dense + BM25 + RRF |
| 完整知识库 | 287 条，210 条 active |
| 知识库 SHA256 | `72a137bcddd9b1a2334e8ec8586e511136c98a3712f3b00e300aa50933a9704e` |
| Excel 源文件 SHA256 | `0298b52d3da7479340742b23936577853a441ff025d6e9acad084f20b22a9d7d` |

密钥未写入测试代码、测试输出或本报告。

## 3. 自动化结果

| 测试集 | 结果 | 状态 |
|---|---:|---|
| 新增业务验收集 `tests/test_acceptance_cases.py` | 51 passed / 1 failed / 52 total | 失败 |
| 全量 Pytest | 114 passed / 1 failed / 115 total | 失败 |
| 知识构建重点校验 | 6 passed | 通过 |
| Python `compileall` | 通过 | 通过 |
| `git diff --check` | 通过 | 通过 |

知识构建重点校验覆盖：Excel 百分比显示、知识库无手机号与交易号、订单表排除、冲突报告、B5 数值检索不暴露科学计数法、确定性重建。

测试过程发现直接运行系统 `pytest` 会误用 Anaconda 环境并导致 `app` 导入失败。项目应固定使用 `.venv.nosync`；这属于执行环境问题，不计为产品缺陷。

## 4. 唯一失败项

| 字段 | 实际结果 |
|---|---|
| 用例 ID | `FAQ-009` |
| 输入 | `AM质地为什么这么稀` |
| 预期 | 命中 FAQ，直接回答；引用源行 62 |
| 实际 decision | `safe_fallback` |
| 实际 citations | 空 |
| 用户影响 | 明明有标准 FAQ，却要求用户补充信息或无法回答 |

### 根因证据

混合检索本身能够找到正确文档：

- 第一名：`AM质地稀`
- 来源：FAQ，Excel 源行 62
- 分数：`0.8588`
- 当前 FAQ 预识别阈值：`0.90`

当前流程先执行范围/意图路由，再进入正式 RAG。该问法没有被识别为明确产品问题，而预检分数 `0.8588 < 0.90`，因此在检索生成前进入安全降级。输入完全等于 FAQ 标题 `AM质地稀` 时分数为 1.0，可正常命中，进一步证明问题位于路由门槛和查询归一化，而不是知识缺失。

建议修复方向：将“是否存在可靠 FAQ 候选”与通用范围路由统一为一次检索决策，并用 FAQ 正负样本校准阈值；不要只为 `AM质地` 增加硬编码关键词。修复后必须复跑全部 115 项。

## 5. 真实 DeepSeek 冒烟测试

| 场景 | 结果 | 说明 |
|---|---|---|
| `我现在很不满意！` | 通过 | `safety_guard -> handoff`，原因为用户情绪激动 |
| `B5洗发水中B5含量百分之多少` | 通过 | FAQ 直答 `0.20%`，引用源行 85 |
| `有什么美白产品推荐？` | 通过 | 调用生成节点并引用可靠产品/FAQ；没有把提亮表述成美白特证 |
| `5%和10%传明酸有什么区别` | 通过 | 生成对比回答并引用产品对比知识 |
| `有去黑头产品推荐吗` | 通过 | 无可靠知识，转人工 |
| `5%传明酸可以和A醇一起用吗` | 通过 | 生成搭配回答，引用产品知识源行 3 |
| `AM质地为什么这么稀` | 失败 | 在进入生成/RAG前被路由为安全降级 |

输出抽查未发现“根据现有资料”“根据产品介绍”“知识库里提到的”“目前资料里”等第三方口吻，也未发现 Markdown 粗体标记。

## 6. 性能采样

本次为本机串行小样本，不等同于生产压测。

| 类型 | 样本 | P50 | P95 | 最大值 | 成功决策 |
|---|---:|---:|---:|---:|---:|
| FAQ 直答 | 20 | 22.1ms | 22.8ms | 23.0ms | 20/20 |
| DeepSeek 综合回答 | 3 | 2263.4ms | 3167.1ms | 3167.1ms | 3/3 |

`NF-001` 通过。`NF-002` 已建立小样本基线，但样本仅 3 条，不能作为容量结论。

## 7. 已验证的关键能力

- 用户主动说“人工/人工服务/不要机器人”等说法时，直接回复转人工确认话术。
- 强负面情绪和不良反应、敏感人群、医美、复杂售后、投诉曝光、敏感信息从安全节点转人工。
- B5 含量、香型、容量、瓶身标识、颜色、到货、油发等事实 FAQ 直接返回，不调用 LLM。
- 推荐、产品对比和搭配进入 DeepSeek 综合生成，且必须携带知识引用。
- “推荐美白产品 → 还有其他的吗 → 这些都可以美白吗”能够继承上下文并避免首轮文档重复。
- 相同消息幂等返回相同 `reply_id`；手机号在会话历史中脱敏。

## 8. 未完成与风险边界

以下内容不能因本轮 99.13% 的自动化通过率而视为已验收：

1. 未接入抖音官方验签、解密、主动发送、重试及真实人工队列，因此未做真实平台端到端测试。
2. 未执行 100 个独立会话、单会话 20 轮和并发压力测试。
3. 未执行至少 50 条真实客服问题的人工语义复核。
4. DeepSeek 故障/超时只由既有 Mock 自动化覆盖，本轮未主动中断真实服务。
5. 本地 Qdrant 存储不能由多个进程同时打开。本轮运行服务与进程内测试共享存储时发生等待，停止服务后恢复；生产应使用 Qdrant Server。
6. Starlette TestClient 对当前 httpx 兼容层产生弃用警告，不影响本轮结果，但依赖升级前需处理。

## 9. 建议的下一步

1. 优先修复 `FAQ-009` 所代表的“可靠 FAQ 被前置范围路由拦截”这一类根因，并新增正负样本防止阈值放宽后误答。
2. 修复后运行全量测试，目标为 `115 passed`，再补齐 161 个用例编号与自动化测试的逐项映射。
3. 使用脱敏后的真实客服问题执行至少 50 条人工语义评审。
4. 获得抖音开放平台凭证后完成真实回调、发送、幂等重试和人工队列联调。
5. 在 Qdrant Server 环境执行并发、长会话和 LLM 超时测试后再进入灰度。

## 10. 复现命令

```bash
PYTHONDONTWRITEBYTECODE=1 \
PYTHONPATH=. \
.venv.nosync/bin/python -m pytest -q tests/test_acceptance_cases.py

PYTHONDONTWRITEBYTECODE=1 \
PYTHONPATH=. \
.venv.nosync/bin/python -m pytest -q

PYTHONDONTWRITEBYTECODE=1 \
PYTHONPATH=. \
.venv.nosync/bin/python -m compileall -q app scripts tests

git diff --check
```
